import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    button: {
        color: '#fff',
        fontSize: 20,
        fontWeight: 'bold',
        backgroundColor: '#000',
        padding: 12,
        borderRadius: 10,
        marginTop: 20
    }
});