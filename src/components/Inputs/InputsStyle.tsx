import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    label: {
        fontSize: 22,
        fontWeight: 'bold',
        marginBottom: 10
    },
    input: {
        height: 40,
        width: 200,
        padding: 10,
        borderColor: '#000',
        borderWidth: 1,
        marginBottom: 10,
        fontSize: 18
    }
});